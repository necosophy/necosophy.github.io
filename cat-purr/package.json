{
  "name": "cat-purr-generator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A calming, glowing particle-cloud cat that reacts to MIDI keyboard input and purrs via synthesized Web Audio.",
  "scripts": {
    "dev": "vite --port 5183",
    "build": "vite build",
    "preview": "vite preview --port 5183"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
